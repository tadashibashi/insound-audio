/**
 * A list of callbacks to provide to the engine for lua scripting.
 * This ensures that the frontend is notified of any changes that
 * occur from calls to script functions.
 */
export interface LuaCallbacks
{

}
